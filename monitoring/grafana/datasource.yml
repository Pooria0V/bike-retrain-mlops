# monitoring/grafana/datasource.yml
# Tells Grafana to connect to our Prometheus instance

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true